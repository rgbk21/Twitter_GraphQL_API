type Query {
    getTweetById(tweetId: [String]): [Tweet]
}

type Tweet {
    id: String
    author_id: String
    created_at: String
    in_reply_to_user_id: String
    text: String
}

type Author {
    id: ID
    firstName: String
    lastName: String
}